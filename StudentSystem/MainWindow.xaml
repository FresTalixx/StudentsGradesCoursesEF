<Window x:Class="StudentSystem.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:StudentSystem"
        Title="Student System" Height="600" Width="900">
    <Window.Resources>
        <local:ImagePathToImageSourceConverter x:Key="ImagePathToImageSourceConverter"/>
    </Window.Resources>
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Left" Margin="0,0,0,8">
            <Button x:Name="BtnAddStudent" Content="Add Student" Margin="0,0,8,0" Click="BtnAddStudent_Click"/>
            <Button x:Name="BtnManageDocument" Content="Student Document" Margin="0,0,8,0" Click="BtnManageDocument_Click"/>
            <Button x:Name="BtnAddCourse" Content="Add Course" Margin="0,0,8,0" Click="BtnAddCourse_Click"/>
            <Button x:Name="BtnAddGroup" Content="Add Group" Margin="0,0,8,0" Click="BtnAddGroup_Click"/>
            <Button x:Name="BtnAddGrade" Content="Add Grade" Click="BtnAddGrade_Click"/>
        </StackPanel>

        <TabControl Grid.Row="1">
            <TabItem Header="Students">
                <DataGrid x:Name="DgStudents" IsReadOnly="True" AutoGenerateColumns="False" SelectionMode="Single" SelectionUnit="FullRow" RowHeight="48">
                    <DataGrid.Columns>
                        <DataGridTemplateColumn Header="Photo" Width="60">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Border Width="36" Height="36" CornerRadius="4" ClipToBounds="True">
                                        <Image Source="{Binding PhotoPath, Converter={StaticResource ImagePathToImageSourceConverter}}"
                                               Stretch="UniformToFill"/>
                                    </Border>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="First Name" Binding="{Binding FirstName}" />
                        <DataGridTextColumn Header="Last Name" Binding="{Binding LastName}" />
                        <DataGridTextColumn Header="DOB" Binding="{Binding DateOfBirth, StringFormat=d}" />
                        <DataGridTextColumn Header="Applied" Binding="{Binding DateOfApplying, StringFormat=g}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
            <TabItem Header="Courses">
                <DataGrid x:Name="DgCourses" IsReadOnly="True" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Description" Binding="{Binding Description}" />
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Groups">
                <DataGrid x:Name="DgGroups" IsReadOnly="True" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                        <DataGridTextColumn Header="Rating" Binding="{Binding Rating}" Width="80"/>
                        <DataGridTextColumn Header="Members" Binding="{Binding Students.Count}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>

            <TabItem Header="Grades">
                <DataGrid x:Name="DgGrades" IsReadOnly="True" AutoGenerateColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Student" Binding="{Binding Student.FirstName}" />
                        <DataGridTextColumn Header="Course" Binding="{Binding Course.Name}" />
                        <DataGridTextColumn Header="Grade" Binding="{Binding NumericGrade}" Width="80"/>
                    </DataGrid.Columns>
                </DataGrid>
            </TabItem>
        </TabControl>
    </Grid>
</Window>
